spring:
  application:
    name: fintech-transfer-service

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/fintech_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: fintech
    password: rootpassword
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect